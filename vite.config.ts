import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

const repoName = 'crs-calculator'; 

export default defineConfig({
  plugins: [react()],
  
 
  base: `/${crs-calculator}/`, 
});